<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/x-icon" href="/img/logo-removebg-preview.png" />
		<title><%=title%></title>
		<style>
			body {
				font-family: Arial, sans-serif;
				background-color: #f4f4f4;
				color: #333;
				display: flex;
				justify-content: center;
				align-items: center;
				height: 100vh;
				margin: 0;
				flex-direction: column;
				text-align: center;
			}
			.container {
				padding: 20px;
				border-radius: 8px;
				box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
				background-color: #fff;
				max-width: 600px;
				width: 90%;
			}
			h1 {
				font-size: 72px;
				color: #00adb5; /* University color */
				margin: 0;
			}
			p {
				font-size: 18px;
				color: #666;
				margin-top: 10px;
			}
			.button {
				margin-top: 20px;
				padding: 12px 24px;
				font-size: 16px;
				color: #fff;
				background-color: #00adb5; /* University color */
				text-decoration: none;
				border-radius: 5px;
				display: inline-block;
				transition: background-color 0.3s;
				border: 1px solid #00adb5;
			}
			.button:hover {
				background-color: white;
				color: #00adb5;
			}
			.illustration {
				margin-top: 20px;
				margin-bottom: 20px;
			}
			.illustration img {
				width: 100px;
				height: auto;
			}
			.danger {
				color: #ef4444;
			}
			.text-danger {
				color: #ef4444;
				font-weight: bold;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="illustration">
				<img src="/img/logo-removebg-preview.png" alt="Politeknik Transportasi Darat Indonesia" />
			</div>
			<% if (alertMessage) { %>
			<p class="text-<%= alertMessage.className %>"><%= alertMessage.message %></p>
			<a href="<%= alertMessage.actionLink %>" class="button"><%= alertMessage.actionLinkLabel %></a>
			<% } else { %>
			<h1>404</h1>
			<p>Oops! The page you're looking for isn't here.</p>
			<p>
				It might have been moved or no longer exists. <br />
				Let’s guide you back to home.
			</p>
			<a href="/" class="button">Back to Home</a>
			<% } %>
		</div>
	</body>
</html>
