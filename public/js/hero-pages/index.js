document.addEventListener("DOMContentLoaded", () => {
	handleSideBar();

	// const promotionsTable = document.querySelectorAll("#promotionsTable th");
	// promotionsTable.forEach((item, index) => {
	// 	item.addEventListener("mouseup", () => sortTable(index, "promotionsTable"));
	// });
});
